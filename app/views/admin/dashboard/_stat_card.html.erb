<%# locals: (label:, value:, value_label:, total:) -%>
<div class="admin-card p-4">
  <p class="admin-subtext text-sm font-medium"><%= label %></p>
  <p class="admin-heading mt-1 text-2xl font-semibold"><%= value %></p>
  <p class="admin-faint mt-1 text-xs"><%= value_label %> · <%= total %> total</p>
</div>
