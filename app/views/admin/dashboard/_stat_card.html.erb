<%# locals: (label:, value:, value_label:, total:) -%>
<div class="admin-card p-4">
  <p class="text-sm font-medium admin-subtext"><%= label %></p>
  <p class="mt-1 text-2xl font-semibold admin-heading"><%= value %></p>
  <p class="mt-1 text-xs admin-faint"><%= value_label %> · <%= total %> total</p>
</div>
