<div class="mb-6">
  <%= link_to "← #{truncate(@article.title, length: 40)}", admin_article_path(@article), class: "text-sm text-zinc-500 hover:text-zinc-700" %>
  <h1 class="mt-2 text-2xl font-semibold text-zinc-950">Edit Article</h1>
</div>

<%= render "form", article: @article %>
