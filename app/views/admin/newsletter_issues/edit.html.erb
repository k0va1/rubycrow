<div class="mb-6">
  <%= link_to "← Issue ##{@newsletter_issue.issue_number}", admin_newsletter_issue_path(@newsletter_issue), class: "text-sm text-zinc-500 hover:text-zinc-700" %>
  <h1 class="mt-2 text-2xl font-semibold text-zinc-950">Edit Newsletter Issue</h1>
</div>

<%= render "form", newsletter_issue: @newsletter_issue %>
