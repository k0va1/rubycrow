RubyCrow - Issue #<%= @newsletter_issue.issue_number %>
<%= "=" * 40 %>

<% @sections.each do |section| %>
<% items = section.newsletter_items.select { |item| item.tracked_link.present? } %>
<% next if items.empty? %>
<%= section.title.upcase %>
<%= "-" * 20 %>
<% items.each do |item| %>
<%= item.title %>
<%= item.article&.blog&.name %>
<%= item.description %>
<%= item.tracked_link.tracked_url(subscriber: @subscriber) %>

<% end %>
<% end %>
---
You're receiving this because you subscribed to RubyCrow.
